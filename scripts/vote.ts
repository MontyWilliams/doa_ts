const index = 0;
async function main(proposalIndex: nunmber) {}

main(index)
    .then(() => process.exit(0))
    .catch((error) => {
        console.logerror(error);
        process.exit(1);
    });
